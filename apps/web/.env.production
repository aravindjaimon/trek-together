# Production build config. Not secret — Vite inlines this into the client bundle,
# so it is public by construction. Committed so `pnpm build -F web` produces the
# same artifact on Vercel, in CI, and locally.
VITE_SERVER_URL=https://trek-together.onrender.com
